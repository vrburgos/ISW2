@model IEnumerable<ISW2PDT5.municipio>

@{
    ViewBag.Title = "Municipio";
}

<div class="panel panel-primary">
    <div class="panel-heading">
        <div class="panel-title">
            Mantenimientos de Municipio
        </div>
    </div>
    <div class="panel-body">
        <div class="btn btn-default linkBlanco">
            @Html.ActionLink("Crear Nuevo", "Create")
        </div>
        <br />
        <br />
        <table class="table">
            <thead>
                <tr>
                    <th>
                        ID
                    </th>
                    <th>
                        Municipio
                    </th>
                    <th>
                        Departamento
                    </th>
                    <th></th>
                </tr>
            </thead>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.id_municipio)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.mun_nom)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.departamento.dep_nombre)
                    </td>
                    <td>
                        <ul class="nav nav-pills">
                            <li role="presentation">@Html.ActionLink("Editar", "Edit", new { /* id=item.PrimaryKey */id = item.id_municipio })</li>
                            <li role="presentation">@Html.ActionLink("Detalles", "Details", new { /* id=item.PrimaryKey */id = item.id_municipio })</li>
                            <li role="presentation">@Html.ActionLink("Eliminar", "Delete", new { /* id=item.PrimaryKey */id = item.id_municipio })</li>
                        </ul>
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
